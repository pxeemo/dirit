[package]
name = "dirit"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
uuid = { version = "1.26.0", features = ["v4"] }

[profile.release]
strip = true
